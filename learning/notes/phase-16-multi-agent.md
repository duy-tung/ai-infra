# Phase 16 — Multi-Agent (chọn lọc)

Chỉ học pattern cốt lõi — đủ để hiểu khi nào multi-agent đáng dùng (và khi nào không).

## Lessons cần capture (chỉ phần này)

- [ ] Supervisor / orchestrator-worker
- [ ] Planner / critic / executor / verifier
- [ ] Failure modes (multi-agent)

## Câu hỏi định hướng

- Khi nào multi-agent **đáng** dùng (fan-out song song, tách context) vs over-engineering?
- Planner/critic/executor/verifier: vì sao tách verifier khỏi executor (giống reviewer/builder)?
- Failure modes riêng của multi-agent: context không share, deadlock chờ tool, chi phí nhân lên.

## Notes

<!-- Dùng _template.md cho mỗi lesson. -->
