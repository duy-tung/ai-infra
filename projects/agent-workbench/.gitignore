# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Trace output (keep the directory, ignore the runs)
traces/*.jsonl

# Sandbox dir created by `make demo` / `make mcp-server`
.sandbox/
